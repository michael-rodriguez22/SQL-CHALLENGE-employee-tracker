const inquirer = require("inquirer")
const prompts = require("../prompts")
const queries = require("../queries")

module.exports = function (view, execute) {
  return {
    departments: async () => {},
    roles: async () => {},
    employees: async () => {},
  }
}
